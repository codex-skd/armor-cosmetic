<h2>v1.0.22 - Default Config Values &amp; Spanish Translation Fix</h2>

<h3>Changes</h3>
<ul>
<li><strong>Fixed default config values</strong>: cosmetic armor button is now <code>20x20</code> by default (was <code>10x10</code>), and the creative inventory button is now <strong>visible</strong> by default (<code>CosArmorCreativeGuiButton_Hidden=false</code>).</li>
<li><strong>Project audit</strong>: removed <code>TEMPLATE_LICENSE.txt</code>, moved templates to <code>src/main/resources/templates/</code>, added <code>temp/</code> to <code>.gitignore</code>, renamed logo to <code>armor_cosmetic.png</code>.</li>
</ul>

<h3>Fix</h3>
<ul>
<li><strong>Corrupted Spanish translation</strong>: fixed UTF-8 encoding of special characters (<code>Ã³</code>&#8594;<code>ó</code>, <code>Ã©</code>&#8594;<code>é</code>) in <code>es_es.json</code>, which affected the config screen labels in Spanish.</li>
</ul>

<hr>

<p><strong>JAR</strong>: <code>armor_cosmetic-26.1.2-neoforge-1.0.22.jar</code></p>
